# include <stdio.h>
# include <cs50.h>

int main(void)
{
    // Counter for how many times?
    int i = 0; // initial 0

    // Using while loop
    // Till i less than 50 do the below
    while (i < 5)
    {
        printf("Hello, world\n");
        // Updating the counter
        i ++ ;
    }
}